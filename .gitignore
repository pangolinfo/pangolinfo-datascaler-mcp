node_modules/
dist/
*.log
.env
.env.*
.DS_Store

# 部署脚本: 含真实 ACR 密码的 .sh 不进仓库; 模板 .example 进.
# 跟 pangolinfo-mcp / ext-scrapeapi 同样的结构 (scripts/window/).
scripts/window/docker-mcp.sh
!scripts/window/docker-mcp.sh.example

# 本地用的 ACK YAML 粘贴模板 (含 redeploy-timestamp 等频繁变的字段)
deploy/k8s-deployment-aliyun-paste.yaml

# Windows 误产物 (printf >/dev/null 在 cmd 下会被解释为创建 nul 文件)
nul

# kubeconfig / 阿里云 cli 的临时凭据
.kube/
*.kubeconfig
.aliyun/
